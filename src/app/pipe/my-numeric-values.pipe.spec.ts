import { MyNumericValuesPipe } from './my-numeric-values.pipe';

describe('MyNumericValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new MyNumericValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
