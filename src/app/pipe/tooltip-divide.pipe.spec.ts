import { TooltipDividePipe } from './tooltip-divide.pipe';

describe('TooltipDividePipe', () => {
  it('create an instance', () => {
    const pipe = new TooltipDividePipe();
    expect(pipe).toBeTruthy();
  });
});
