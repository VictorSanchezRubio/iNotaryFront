import { MyFilteronceptosPipe } from './my-filteronceptos.pipe';

describe('MyFilteronceptosPipe', () => {
  it('create an instance', () => {
    const pipe = new MyFilteronceptosPipe();
    expect(pipe).toBeTruthy();
  });
});
